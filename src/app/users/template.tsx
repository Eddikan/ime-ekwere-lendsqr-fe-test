// src/app/user/template.tsx

import React from "react";

const UserTemplate = ({ children }: { children: React.ReactNode }) => {
  return <section>{children}</section>;
};

export default UserTemplate;
